class <%= name.to_screen_name %> extends Tres.Screen
  id        : "<%= name.downcase.dasherize %>"
  template  : _.template(JST['<%= name.downcase %>'])

window.<%= name.to_screen_name %> = <%= name.to_screen_name %>